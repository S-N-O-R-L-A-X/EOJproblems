#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    int i,weight,t;
    char cmp[10][]={zero,one,two,three,four,five,six,seven,eight,nine};
    int plus1[9];
    int plus2[9];
    char str[200];
    scanf("%d",&t);
    for(i=0;i<=t-1;i++)
    {
        gets(str);
        len=strlen(str);
        for(j=0;j<=len-1;j++)
        {
            if(str[j]==' ')
            {

            }
        }

    }
    return 0;
}
