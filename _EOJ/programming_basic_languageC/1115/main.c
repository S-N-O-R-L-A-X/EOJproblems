#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n,m,ox,i,j,k,l;

    scanf("%d %d",&n,&m);
    int cow[n][m];
    for(i=0;i<=n-i;i++)//
    {
        scanf(" %d",&ox);
        for(j=0;j<=ox-1;j++)
            scanf(" %d",&cow[i][j]);
    }

    return 0;
}
